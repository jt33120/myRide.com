specVersion: "v1beta"
serviceId: "trustcycle"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "trustcycle-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
